# FixHome Mobile — Audit Booking → ServiceOrder cho Customer và Technician

**Mốc kiểm tra:** 23/09/2026, cuối ngày ICT. **Phạm vi:** UI, điều hướng và hợp đồng API trong worktree Mobile cá nhân đang phát triển; đối chiếu Backend `dev` và Master Spec v1.4. Đây là báo cáo **để nhóm review/chia task**, không phải chứng nhận release, không phải kết quả QA end-to-end. Không chứa tài khoản, token, địa chỉ cụ thể hay dữ liệu khách hàng.

## 0. Cách đọc kết quả và source-of-truth

- `S`: Đã có source UI/controller gọi API hoặc helper tương ứng; unit/source review từng slice đã có. `B`: Có chứng cứ Chrome cho một phần cụ thể. `M`: Màn demo/static cũ, không được dùng làm bằng chứng thực thi thật. `G`: Thiếu UI/caller hoặc hành động chưa nối; API tồn tại không có nghĩa tính năng Mobile đã hoàn thành. `V`: Chưa có kết quả E2E thực trên thiết bị/API/provider. `D`: Phụ thuộc owner/team khác hoặc quyết định nghiệp vụ, không tự mở phạm vi.
- **Ưu tiên kỹ thuật:** `P0` chặn hành trình chính/tích hợp hoặc có nguy cơ hiển thị sai nghiệp vụ; `P1` chặn nhánh lỗi/thao tác thiết yếu; `P2` hậu mãi và polish. Đây là thứ tự xử lý đề xuất, không phải nhận định tất cả lỗi đã tái hiện trên thiết bị.
- Mobile source kiểm tra: branch `foxonworld/booking-mobile-serviceorder-20260922`, HEAD baseline `6a640f6`; ở mốc audit có **62 đường dẫn staged/modified/untracked** chưa vào commit. Source mới hiện diện trong worktree này, **không phải đã có trên GitHub main/dev**. Repo Mobile local `main` còn ở SHA cũ `612e5f9`, origin/main và personal branch mới hơn ở `6a640f6` vào mốc kiểm tra; re-fetch trước bất kỳ PR nào.
- Backend: đọc tại local dev HEAD `21646da` cho routes/gates, không tự coi Backend đang chạy ngày sau có cùng HEAD. Master Spec `docs/docs/FIXHOME-Master-Project-Specification-v1.4.md`: §7, §8.5–8.9, §8.15–8.22, §9–10. **Xung đột tài liệu cần sync:** Master Spec v1.4 còn câu tối đa **5** kỹ thuật viên, nhưng owner/team đã chốt **đúng 2 có thứ tự ưu tiên** và Backend/Mobile đang thực thi quy tắc mới; không tự nâng lại thành 5. Spec mô tả Invoice sau customer confirm ở đoạn high-level; Backend hiện tạo invoice UNPAID tại technician request-completion, khách confirm và payment verified là các bước riêng: UI phải theo Backend hiện hành và cần team đồng bộ docs.
- Lỗi chuẩn hóa mã quận/phường của Backend đang có thành viên khác nhận. Audit chỉ ghi dependency và testcase hồi quy; **không tự sửa hoặc đổi khu vực thợ**.

## 1. Bảng audit Customer — từ khám phá đến sau hoàn thành

| ID / mức | Bước & hành động người dùng mong đợi | Mobile hiện tại & source cụ thể | Trạng thái / lỗ hổng / hành động cần làm | Điều kiện đạt tối thiểu |
|---|---|---|---|---|
| C01 P0 | Mở danh mục/tìm dịch vụ → chọn đúng dịch vụ trong catalog | `CustomerServicesScreen.tsx` tự tạo `getAllServices()` với ID minh họa `1...`; `CustomerServiceDetailScreen.tsx` luôn hiển thị một dịch vụ/giá tĩnh rồi qua AI Diagnosis. `CustomerBookingCreateScreen.tsx` ngược lại dùng API catalog thật. | **M+G**: đường vào từ danh mục không mang service UUID hợp lệ; giá mô tả có thể làm người dùng hiểu sai. Giao Customer Discovery owner sửa catalog/route param; không sửa form API đã có để che lỗi. | Chạm mọi danh mục dịch vụ → detail đúng service ID + giá/tên từ API → form Booking được prefill đúng ID; chưa có giá thì nói chưa rõ, không bịa giá. |
| C02 P1 | Xem hồ sơ thợ, listed labor price, warranty tham khảo, khoảng cách trước Booking; có thể liên hệ trước Booking theo policy | `CustomerMatchingScreen.tsx` sau Booking chỉ hiển thị tên, rating và khoảng cách (nếu có); không có entry rõ ràng tới profile/listedLaborPrice/typicalWarranty; chat `ChatList/Thread` tồn tại nhưng pre-booking contact chưa chứng minh. | **G+V+D**: UI so sánh thợ và pre-booking chat chưa nối; policy trả lời trước Booking theo Spec có điểm chưa khóa. | Danh sách thợ hiển thị dữ liệu được phép từ API, xem profile/thông tin so sánh; pre-booking chat chỉ khi BE/policy cho phép; không lộ Booking private. |
| C03 P1 | AI gợi ý hoặc khách chọn dịch vụ thủ công → đi form thật | `CustomerAIDiagnosisScreen.tsx` đã điều hướng `CustomerBookingCreate` với prefill; form chỉ nhận service tồn tại trong catalog. | **S, V**; test đường đi AI/chat → form, đổi service thủ công, back/forward, logout. | Không tự tạo Booking sau AI, không ép dùng AI, prefill không vượt quyền. |
| C04 P0 | Chọn địa chỉ hợp lệ, cảnh báo địa chỉ không đủ dữ liệu khu vực trước tạo đơn | `CustomerBookingCreateScreen.tsx` lấy saved addresses và chỉ kiểm tra UUID; `CustomerProfileScreen.tsx` lưu địa chỉ và có fallback quận/tỉnh; geocode failure có lúc im lặng. | **G+D**: không phân biệt placeholder `Tỉnh/TP`, `Quận/Huyện` hay mã quận cũ với vùng phục vụ chuẩn; Backend fix do teammate khác; Mobile cần API contract/empty-state/validation sau khi fix. | Địa chỉ không thể match phải báo lỗi vùng rõ ràng trước POST; sửa lại địa chỉ và thử được; khu vực phù hợp thực tế, không auto map toàn quận sang một phường. |
| C05 P0 | Chọn service, mô tả, ngày/khung giờ, quantity/urgency, tạo duy nhất một Booking | `CustomerBookingCreateScreen.tsx`, `booking-window.ts`, `bookingsApi.createBooking`: có form, 2h window, mặc định NORMAL, request in-flight/ambiguous guard; thiếu điều khiển quantity, urgency trên UI. | **S+B+G**: user đã tạo Booking trên Chrome; quantity/urgency cố định/không hiển thị tùy chọn; không có flow sửa thông tin trong form khi POST ambiguous ngoài hướng dẫn liên hệ. | Một POST tạo đúng 1 Booking; thời gian ngày/giờ/timezone và dữ liệu bắt buộc đúng; lựa chọn quantity/urgency theo phạm vi MVP đã chốt, kết quả timeout được reconciliation an toàn. |
| C06 P1 | Chụp/chọn ảnh sự cố đính kèm Booking riêng tư | `bookingsApi.createBooking` có `photoUploadIds`/`attachMedia`, nhưng form hiển thị rõ ảnh AI **chưa đính kèm** và không có Booking photo picker/upload chính thức. | **G+D**: ảnh Booking qua private signed-upload chưa nối; không dùng dataURI AI như media đã upload. | Chỉ gửi uploadId thật của Customer có quyền; preview/huỷ/xác minh upload và lỗi provider, không phát tán ảnh private. |
| C07 P0 | Sau tạo Booking mở candidates từ ID thật | `CustomerBookingCreateScreen` success → `CustomerMatching {bookingId}`, `bookingsApi.getBooking/getCandidates`. | **S+B+V**: tạo Booking & GET200[] có bằng chứng Chrome; chưa xác nhận danh sách thực sau bản sửa khu vực bằng authenticated GET. | Đúng bookingId/owner/area/time, danh sách thợ thật; no candidates có phân loại nguyên nhân. |
| C08 P0 | Chọn chính xác 2 thợ theo thứ tự ưu tiên, so sánh và gửi mời một lần | `CustomerMatchingScreen.tsx`, `booking-candidate-selection.ts`, API `sendShortlist`; UI có rating/distance nhưng chưa trình bày listed labor price/warranty mặc dù response type có thể hỗ trợ. | **S+V**; source chặn thợ thứ 3 và POST trùng, chưa mobile E2E 2-role; thiếu so sánh phí/bảo hành để khách hiểu lựa chọn. | POST chính xác hai technician **User UUID** có thứ tự, không gửi hai lần; app không tự tuyên bố Tech đã Accept. |
| C09 P0 | Xem thợ ưu tiên 1 PENDING, ưu tiên 2 STANDBY, hết hạn/decline → activation/reselect | `CustomerMatchingScreen.tsx` dùng GET Booking + invitations, waiting copy và **manual refresh**; `CustomerBookingsScreen` hiển thị MATCHING, resume SUBMITTED. | **S+G+V**: thiếu timeline/ETA rõ, push/live refresh; chưa E2E 1 decline→2 active→Accept, timeout/re-match/closed rounds. | Trạng thái cập nhật đúng; Tech #2 không thấy standby trước activation; mọi nhánh kết thúc có CTA tiếp tục hoặc điều phối, không mời trùng. |
| C10 P1 | Khách đổi lịch/mô tả trước khi sửa | `bookingsApi.reschedule` có API; **không có caller trong Customer screens hiện tại**. Backend có POST reschedule với trạng thái/time gate. | **G**: cần màn/sheet đổi lịch, review ảnh hưởng vòng mời, lỗi lịch thợ/conflict. | Chỉ cho phép đúng trạng thái backend; thay giờ reset matching nếu server yêu cầu; không giả lập giữ thợ/lịch cũ. |
| C11 P1 | Khách chủ động hủy Booking trước khi có ServiceOrder | `bookingsApi.cancelBooking` có API nhưng **không có caller screen**. | **G**: thêm nút và bước xác nhận lý do trên Booking detail/history với copy hậu quả theo state. | 1 POST reason, read-back trạng thái; không coi hủy Booking là hủy ServiceOrder và không tự áp strike/phí. |
| C12 P0 | Sau Tech Accept mở đúng ServiceOrder, không lẫn Booking ID | `CustomerBookingsScreen` crosswalk/pagination → `CustomerOrderDetail {serviceOrderId}`; `CustomerMatching` chỉ in `serviceOrderId` dạng selectable, không CTA vào detail ngay tại màn matching. | **S+G+V**: đường qua history có thật nhưng thiếu CTA/transition nhất quán khi đang chờ chuyển sang MATCHED. | Accept tạo đúng 1 ServiceOrder, history/detail mở bằng order ID, customer được dẫn vào chi tiết thực không vào `CustomerTechFound` demo. |
| C13 P1 | Theo dõi thợ trên đường, check-in, sự cố GPS/no-show | `CustomerOrderDetail` status/timeline server-backed một phần; `CustomerTrackingScreen` vẫn là bản đồ minh họa. API cập nhật location có ở Backend, Mobile không có customer tracking real-time được chứng minh. | **M+G+D**: quyết định mức live tracking/privacy trước khi code, ít nhất thông báo EN_ROUTE/check-in VALID/không chính xác. | Không phát tọa độ giả, chỉ hiển thị dữ liệu Backend cho đúng order/role; khi không có vị trí hiển thị trạng thái trung thực. |
| C14 P0 | Nhận báo giá, xem từng dòng labor/parts/source/warranty và APPROVE/REJECT | `CustomerOrderDetailScreen` và `customer-quotation-decision.ts` đã gọi `ordersApi.approveQuotation/rejectQuotation`, paid warranty mặc định OFF, cảnh báo REJECT đóng cả order. | **S+V**: unit/review từng slice đạt; phần FixHome-provided Parts catalog/pickup/warranty chưa hoàn chỉnh; phải test giá, item approval qua API thực. | Quote đúng SENT, Customer xác nhận 2 bước, BE result read-back, không tự tính/chốt paid; REJECT kết thúc đúng order theo BE. |
| C15 P0 | Trong UNDER_REPAIR duyệt/từ chối từng yêu cầu chi phí phát sinh | `CustomerOrderDetail` + `customer-additional-cost-decision.ts`: chỉ cho current LABOR, PENDING, chưa hết TTL; GET reconcile; có 2 bước xác nhận. | **S+V+G**: parts/additional warranty chưa cover; cần kiểm tra cost REJECT không hủy base order và expiry/refetch. | Không quyết định nhầm request hoặc gửi hai lần; APPROVE cộng tổng do BE; REJECT chỉ riêng cost theo rule đã duyệt. |
| C16 P0 | Thợ báo xong → khách thấy yêu cầu nghiệm thu với ảnh AFTER, hóa đơn UNPAID | `CustomerOrderDetail` GET evidence/invoice/completionRequestedAt, **chưa có hành động confirmCompletion**; form/trạng thái nghiệm thu chuyên biệt chưa có. | **G+V**: phải hiển thị rõ work requested ≠ customer confirmed ≠ paid ≠ completed; thiết kế đường xử lý nếu khách từ chối nghiệm thu/tranh chấp cần team chốt. | Chỉ hiện CTA khi đúng trạng thái; xem ảnh sau làm, chi phí và điều kiện; lỗi phải có hướng xử lý, không tự COMPLETED. |
| C17 P0 | Khách bấm xác nhận hoàn thành một lần | `ordersApi.confirmCompletion` có method, không được gọi từ screen; Backend xác nhận không tự charge. | **G+D**: làm controller+screen+tests, hỏi team UX về feedback/rating/signature optional; không bịa chữ ký hoặc lồng review bắt buộc. | Auth đúng Customer, trạng thái+request hợp lệ, POST once/reconcile, BE confirmation được lưu và chỉ COMPLETED khi payment hợp lệ. |
| C18 P0 | Khách xem hóa đơn và thực hiện thanh toán online theo provider đã duyệt | Invoice GET hiển thị thật; `ordersApi.payInvoice` có method nhưng không có caller screen; `CustomerCompletedScreen` cũ chuyển sang review, **không thu tiền**. | **G+D**: payment/provider/billing riêng với BE/Finance owner; không bật CTA dựa trên mock hoặc tự set PAID. | Tạo payment theo idempotency, hiển thị pending/fail/cancel/verified, chỉ dùng server/webhook verify mới đánh dấu paid; refund/rollback do owner khác. |
| C19 P1 | Thanh toán tiền mặt: khách xác nhận số thợ khai và khiếu nại sai lệch | API cash settlement đã tồn tại trong ordersApi, không có UI caller; Spec yêu cầu Technician declare/Customer confirm/Manager xử lý bất đồng. | **G+D**: quyết định finance/team trước khi triển khai; không tự xác nhận timeout. | Số tiền customer confirm từ order thật; PENDING/CONFIRMED/DISPUTED được phân biệt; không tự PAID chỉ vì bấm. |
| C20 P1 | Khách hủy ServiceOrder trước/sau arrival; xin hỗ trợ tranh chấp | `ordersApi.cancelOrder` có method nhưng không caller screen; `CustomerUnderRepairScreen` demo chuyển thẳng Completed. | **G+D**: quy tắc hậu quả theo trạng thái, strike/SM exception, tránh nhầm Booking cancel; cancellation reason có lưu. | BE quyết định trạng thái/penalty; front copy chính xác pre/post arrival và không tự tạo hoàn tiền/phạt. |
| C21 P1 | Sau thật sự COMPLETED khách gửi đánh giá liên kết đơn | `CustomerReviewScreen.tsx` hiện mặc định 5 sao/nội dung sẵn; `handleSubmit()` chỉ `Alert` thành công, **không gọi API** dù `ordersApi.submitReview` có. | **M+G**: nguy cơ đánh lừa user đã gửi review; thay bằng form trên order ID thật, hoặc gỡ đường vào demo. | POST 1 review/real completed order, xung đột review cũ/deny/fail không giả báo thành công; tránh ghi rating hai lần nếu confirm chứa rating. |
| C22 P1 | Sau hoàn thành xem bảo hành/lịch sử/đòi bảo hành | `ordersApi.getWarranties/getRepairHistory` có client; `CustomerOrderDetail` hiện không có warranty-claim CTA/contract đầy đủ trong phạm vi đã audit. | **G+D**: coverage riêng labor/FixHome parts/tech paid parts, claim và new unrelated issue, SM resolution cần owner. | Coverage đúng server, claim chỉ khi đủ coverage và đúng provider; lịch sử/ref từ server, không bịa ngày bảo hành. |
| C23 P2 | Thông báo mời thợ, tiến độ, quote, complete, payment | CustomerNotifications GET có source; chưa chứng minh Deep-link tới booking/order + read-all/mark-read E2E. | **S+V+G**; thống nhất event contract/realtime với Backend/messaging owner. | Mở notification → đúng Booking/ServiceOrder và actor, unread/read states phản ánh API. |

## 2. Bảng audit Technician — từ inbox đến kết thúc

| ID / mức | Bước & hành động mong đợi | Mobile hiện tại & source cụ thể | Trạng thái / lỗ hổng / hành động cần làm | Điều kiện đạt tối thiểu |
|---|---|---|---|---|
| T01 P1 | Nhận lời mời khi đến lượt, biết service/khu vực/khung giờ/hết hạn | `TechnicianInvitationsScreen.tsx`/`invitation-inbox.ts` GET `/invitations/my`; preview đã allowlist riêng tư. | **S+V**: chỉ source review, thiếu notification/deep-link và thời gian đếm ngược/refresh khi standby→active. | Tech #2 không nhìn thấy invitation standby quá sớm; stale/expired không cho Accept; không lộ address/media trước Accept. |
| T02 P0 | Tech ACCEPT/DECLINE; sau ACCEPT mở ServiceOrder thật | Inbox có real respond API + lock; sau ACCEPT điều hướng về `TechnicianMain`/Jobs, không mở thẳng detail ID trong phần đã kiểm. | **S+G+V**: UX bàn giao accept→assigned order chưa trực diện, cần xác minh only-one-order race và focus refresh. | ACCEPT nhận một order ID thật, có CTA mở detail; DECLINE kích hoạt thợ dự phòng đúng rule, không strike. |
| T03 P0 | Danh sách việc mới/cũ và quyền truy cập sau thay thợ | `TechnicianJobsScreen`, `TechnicianOrderDetail`: real GET/pagination/focus; historical cards sanitize, guard content. | **S+V**: chưa live E2E thay assignment/404/403; `TechnicianHomeScreen` vẫn có list GET riêng dễ lệch trạng thái. | Đơn mới hiện sau ACCEPT, không lộ địa chỉ/phone/quote của đơn cũ, Home/Jobs đồng bộ. |
| T04 P1 | Nhấn bắt đầu di chuyển EN_ROUTE, xem lịch và liên hệ khách hợp lệ | `TechnicianJobsScreen` có `ordersApi.enRoute`; chi tiết hiển thị server-backed contact với active assignment. | **S+V**: chưa native smoke, số điện thoại/hành động call/chat và cập nhật ETA chưa được audit hết. | Trạng thái được Backend xác nhận, Customer thấy EN_ROUTE, không tự hiển thị ETA giả. |
| T05 P0 | Check-in GPS khi đến, xử lý không cấp quyền/accuracy/geofence | `TechnicianJobsScreen` + `technician-check-in.ts` dùng expo-location foreground + `ordersApi.checkIn`; chỉ `result VALID` mới unlock BEFORE. | **S+V**: Chrome không chứng minh native permission; no-location/manual exception phải theo BE/SM policy. | Android/iOS foreground permission, accuracy/result, 401/403/409 và denial/outside flow; không tự giả `VALID`. |
| T06 P0 | Chụp/chọn ảnh BEFORE sau check-in VALID, GET ảnh đúng quyền | `TechnicianOrderDetail` + `technician-evidence-upload.ts`, `ordersApi.uploadEvidenceBefore/getEvidence` có picker/multipart/source QA. | **S+V**: chưa provider/native E2E; camera, file MIME/size 10MB, 503, short-lived signed URL phải smoke. | Ảnh trước upload 1 lần, GET lại ảnh signed, không lộ `storage://` hoặc reuse across orders. |
| T07 P0 | Khảo sát: FIXED_PRICE được dùng snapshot hợp lệ; INSPECTION_REQUIRED lập báo giá | `technician-quotation-create.ts`/screen thực hiện real quote multi-line labor+technician parts, warranty option; FIXED_PRICE start-repair gate source có. | **S+V+G**: chưa E2E cả hai pricing mode; FixHome-provided part/catalog/pickup flow còn thiếu. | Backend quyết định yêu cầu báo giá; không ép fixed-price tạo quote hoặc bỏ qua điều kiện evidence/arrival. |
| T08 P1 | Gửi báo giá, theo dõi CUSTOMER APPROVE/REJECT/SUPERSEDED | `ordersApi.createQuotation`, tech detail controllers; có refetch và ambiguity guard. | **S+V**: thiếu notification và cách thể hiện rõ lý do đóng đơn khi customer REJECT. | Chỉ một quote hợp lệ được gửi; APPROVE mở start; REJECT không tiếp tục repair; không tự xem submit là accept. |
| T09 P1 | Lấy part FIXHOME, xác nhận pickup/đã dùng/chưa dùng, bảo hành part source | Editor hiện có TECHNICIAN parts; chưa có caller Mobile cho catalog FixHome/pickup/used-vs-unused. | **G+D**: tách task Parts/Service Manager contract; không bịa giá/hoa hồng, không tính part unused vào invoice. | Source/price/warranty/usage snapshot đúng Backend và team-approved; không mở inventory engine ngoài MVP. |
| T10 P0 | Bắt đầu sửa chữa khi đã đủ điều kiện | `TechnicianOrderDetail` + `technician-start-repair.ts` gọi `ordersApi.startRepair`, confirm, reconcile. | **S+V**: chưa live authentic status transition, customer quote/part edge cần smoke. | Đúng EN_ROUTE/VALID/BEFORE/pricing approval; Backend trả UNDER_REPAIR, không double POST khi mất mạng. |
| T11 P1 | Trong UNDER_REPAIR tạo yêu cầu chi phí phát sinh và theo dõi phản hồi | `technician-additional-cost-create.ts`/tech detail thật, **LABOR-only**; display GET requests. | **S+V+G**: PARTS/additional warranty/revise/expired, no-agreement unable-to-continue cần branch theo policy; không tự cộng tiền trước customer approval. | POST once; Customer chỉ quyết request cụ thể, REJECT không hủy toàn bộ base order mặc định; pending TTL rõ. |
| T12 P0 | Ảnh AFTER, note hoàn thành và yêu cầu hoàn thành | `technician-after-evidence-upload.ts`, tech detail; `technician-request-completion.ts` được wire nhưng `__DEV__` only, 2 confirm; Backend tạo invoice UNPAID. | **S+V+D**: chưa authentic E2E sau AFTER; completion note DTO có nhưng UI source hiện gọi requestCompletion **không truyền note**, cần thống nhất có bắt buộc cho MVP không; release chưa có CTA do dev gate. | AFTER đủ min, pending approval resolved, POST once, invoice UNPAID và completionRequestedAt server-owned; nếu cần phát hành chức năng cho release phải review/gate riêng. |
| T13 P0 | Sau request: thấy trạng thái chờ nghiệm thu/thu tiền/COMPLETED đúng | Tech detail hiển thị server order/invoice; không có full tail experience hiện hữu/đã test. | **G+V**: chưa nối Customer confirm/payment; Tech không nên bấm legacy complete để bỏ qua gate. | Khi chưa confirm/payment vẫn UNDER_REPAIR + milestones; khi both satisfied BE mới COMPLETED; Không tự mark PAID. |
| T14 P1 | Tech hủy sau ACCEPT, không làm được, xin đổi thợ/sự cố sau arrival | `ordersApi.cancelOrder` có API nhưng chưa có màn Tech caller; Backend route có quy tắc pre/post arrival & SM review. | **G+D**: cần status-aware cancel/strike warning và escalation; không tự phân tiền hoặc dùng Decline của lời mời để hủy ServiceOrder. | Có reason, server decides rematch/SM case, giữ assignment history; không auto-strike Tech chỉ vì từ chối lời mời. |
| T15 P1 | Khai tiền mặt, số tiền nhận, trả PlatformDue, xem trạng thái nghĩa vụ | `ordersApi.declareCashSettlement/getCashSettlement` có client; màn thợ không gọi. | **G+D**: finance/provider/SM cần decision & test độc lập, không chạy giả PAID. | Cash declare khác customer confirm; PlatformDue theo server snapshot và status riêng, không tự release order sai. |
| T16 P1 | Xem job trước đây, doanh thu/commission thực, quyền xem history | Tech Jobs lịch sử được sanitize; invoice GET riêng; chưa có màn rõ dòng tiền/công nợ theo order. | **S+G+V**: dữ liệu history hạn chế đúng privacy nhưng cần đường đọc đúng phần summary được phép; finance owner. | Tech cũ chỉ thấy summary được phép, không thấy customer private; nghĩa vụ/thu nhập đến từ BE. |
| T17 P1 | Xử lý bảo hành đã được giao, claim/parts, tranh chấp | `TechnicianOrderDetail` chưa có full warranty-claim/action flow trong phạm vi audit. | **G+D**: claim chỉ khi coverage; provider cho parts khác nhau; SM xử lý dispute. | Tech chỉ nhận claim thuộc trách nhiệm; trạng thái & evidence/ownership được xác minh. |
| T18 P2 | Nhận thông báo realtime: lời mời, quote, cost, completion | `TechnicianNotificationsScreen.tsx` vẫn là các thẻ **tĩnh**, có màn chat API/socket riêng. | **M+G+V**: tránh báo giả/không có link đến đơn; cần chia riêng với Messaging owner. | Notification dựa trên BE, vào đúng order/invitation, unread/timeout/auth đúng. |

## 3. Vấn đề xuyên suốt, điều hướng và dữ liệu

| ID / mức | Quan sát / ảnh hưởng | Phần cần audit hoặc sửa | Owner đề xuất |
|---|---|---|---|
| X01 P0 | Các màn demo `CustomerTechFound/Tracking/Quotation/UnderRepair/Completed/Review` vẫn registered ở `AppNavigator.tsx`, trong khi luồng mới dùng CustomerOrderDetail; auth branches còn đăng ký cả screen của role đối diện. | Lập **reachability map** mọi CTA/deep-link/tab/back sau sign-in/out; role-aware navigation và server auth; ẩn/chuyển hướng hoặc đổi demo thành read-only sample rõ ràng, **không xóa vội màn đồng đội dùng**. | Mobile integrator + Customer UX |
| X02 P0 | Backend+data khu vực cũ/quận ↔ mới/phường đã gây API candidates200[] ở Web/Mobile. | Teammate BE owner đang fix; Mobile kiểm tra contract area error / fail-closed saved address + regression. Không mở parallel Backend writer; không đổi mã thợ đại trà. | Backend geo owner (ngoài task này) + Mobile integration QA |
| X03 P0 | Chưa có E2E Customer→Tech ACCEPT→Order→AFTER→UNPAID invoice→Customer confirm→payment→COMPLETED, dù từng slice unit-pass. | Test trên **một** Booking/ServiceOrder hợp lệ và hai account test, chụp response/state từng bước; không dùng Chrome thay thế native camera/GPS; payment/provider qua finance gate riêng. | E2E QA + owner từng module |
| X04 P0 | 62 worktree entries staged/unstaged/untracked chưa commit, khác GitHub main; code chồng lên các shared API/nav/detail screen. | Bảo toàn worktree, review tổng hợp cross-slice/ownership; tạo commit local có danh sách file rõ, không `git add .` hoặc commit teammate/secret, không PR/merge/deploy khi chưa chốt review. | Mobile integrator |
| X05 P1 | POST timeout/ambiguous: source guard tốt từng slice, nhưng app restart/đa thiết bị/realtime cạnh tranh chưa E2E. | Ma trận idempotency/reconciliation cho create Booking, shortlist, accept, upload, quote, cost, start-repair, completion, confirm/pay; backend authoritative. | Mobile integrator + Backend contract QA |
| X06 P1 | 401/403/404/409/422/503, offline, session switch, notification read, stale signed media, no technician/expired/declined có thể hiển thị giống empty/hướng sai. | QA table status-by-role/error, không tự retry POST; mỗi màn có CTA giải quyết cụ thể và không lộ data role cũ. | UX/error-state + QA |
| X07 P1 | Giá trước Booking, listed labor/typical warranty, part warranty/finance copy có chỗ mock hoặc thiếu. | Lấy dữ liệu thật, hiển thị UNKNOWN/estimate khi chưa chốt; so spec §7/§8.3/8.14–8.17, không hiển thị tiền đã thu nếu invoice UNPAID. | Customer UX + Finance/Parts owner |
| X08 P1 | React Native native/photo/GPS/push/storage khác Expo Web; Web map đang cố ý là placeholder, Chrome sessionStorage khác SecureStore native. | Android+iOS test riêng; build phù hợp Expo Go/dev build; kiểm permission, GPS quality, multipart/private photo 503, network IP/CORS và push. | Native QA |
| X09 P1 | Dữ liệu spec còn max5 invitations và một số status/hoá đơn không phản ánh rule đang thực thi đúng2. | Team docs owner reconcile approved rules vs Backend source và latest decisions; chỉ sửa canonical docs sau owner approve, **không tự đổi Backend theo mô tả cũ**. | BA/leader/docs |
| X10 P2 | TechnicianHome và CustomerHome có phần nguồn dữ liệu/CTA khác với Jobs/History, dễ thấy badge/order count cũ. | Kiểm focus refresh, pagination, empty/error, tab counts/crosswalk và deep-link sau accept, login/logout. | UX navigation + QA |

## 4. Phân biệt mức độ đã xác minh

- **Đã chạy trong worktree hiện tại (23/09 cuối ngày):** Jest **29/29 suites, 762/762 tests PASS**; `npm run typecheck` PASS; `npm run check:expo` PASS; `git diff --check HEAD` PASS; targeted ESLint 12 file Booking/API/navigation/Web storage quan trọng PASS. `npm run lint` **FAIL 4 lỗi `import/no-unresolved` ở 4 file CustomerHome, TechnicianKyc, TechnicianProfile, chat-socket**, khác với bản snapshot cũ báo 7; phải xử lý/vet baseline riêng. Test không chứng minh UX/navigation/device/provider.
- **Chrome thực tế đã quan sát:** mở Expo Web, qua lỗi SecureStore bằng adapter, Customer đăng nhập và tạo Booking; GET candidates trả [] do lỗi mã khu vực. Hai Booking test đã được chỉnh snapshot riêng để test, **chưa nhận screenshot xác nhận danh sách/thao tác tiếp sau chỉnh**. Không dùng hai đơn đồng thời để tạo lời mời trùng.
- **Chưa chạy:** native Android/iOS full flow, check-in GPS thực, upload private storage thật, hai-role authenticated Accept→Order, request-completion tạo invoice UNPAID thực qua Mobile, Customer confirm/payment/COMPLETED, Finance/cash/provider, warranty/review E2E. Không tự đánh dấu `DONE` từ việc API tồn tại hoặc unit PASS.

## 5. Các gói việc và cách merge an toàn

Xem `docs/BOOKING_SERVICEORDER_MOBILE_TASK_BOARD_2026-09-23.md` để có mã task, owner theo **vai trò đề xuất**, phạm vi file, dependency, điều kiện nghiệm thu, test/PR plan và phương án gom commit. Không có tên người nhận task nào được coi là đã phân công cho đến khi leader xác nhận. Đây là **bản audit chứ không phải yêu cầu tạo ticket tự động**.
